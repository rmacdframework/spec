{
  "description": "RMACD session governance: every tool call is evaluated against the bound RMACD profile via the rmacd-framework SDK (pip install \"rmacd-framework>=0.14\"). Unbound sessions pass through untouched with a one-time notice; bound sessions fail closed on errors — including when the SDK itself is missing. See /rmacd:status and docs/claude-code.md in the spec repo.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${RMACD_PYTHON:-python3}\" \"${CLAUDE_PLUGIN_ROOT}/hooks/rmacd_session_start.py\"",
            "timeout": 10
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "\"${RMACD_PYTHON:-python3}\" \"${CLAUDE_PLUGIN_ROOT}/hooks/rmacd_guard.py\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
